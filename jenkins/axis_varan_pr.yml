# axis_varan_pr.yml
# matrix of builds tested for pull requests on varan
COMPILER:
  - intel
  - gcc

BUILD_TYPE:
  - base
  - serial
  - thrust
  - flecsi
  - readme

exclude:
  - COMPILER: intel
    BUILD_TYPE: flecsi
  - COMPILER: gcc
    BUILD_TYPE: readme

