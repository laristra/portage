# axis_varan.yml
# matrix of builds tested nightly on varan
COMPILER:
  - intel
  - gcc

BUILD_TYPE:
  - base
  - debug
  - serial
  - thrust
  - flecsi
  - coverage

exclude:
  - COMPILER: intel
    BUILD_TYPE: flecsi
  - COMPILER: intel
    BUILD_TYPE: coverage

