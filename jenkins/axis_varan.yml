# axis_varan.yml
COMPILER:
  - intel
  - gcc53

BUILD_TYPE:
  - base
  - thrust
  - flecsi
  - coverage

exclude:
  - COMPILER: intel
    BUILD_TYPE: flecsi
  - COMPILER: intel
    BUILD_TYPE: coverage

